<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ThivashGram - Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #0b0c10;
            color: #ffffff;
            display: flex;
            min-height: 100vh;
        }

        /* --- DESKTOP SIDEBAR --- */
        .sidebar {
            width: 250px;
            background-color: #12131c;
            border-right: 1px solid #1f2029;
            padding: 30px 20px;
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
        }

        .brand-logo {
            font-size: 2rem;
            font-weight: bold;
            font-style: italic;
            background: linear-gradient(45deg, #f72585, #7209b7);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 40px;
            padding-left: 10px;
        }

        .nav-links {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 12px 15px;
            color: #a5a7b4;
            text-decoration: none;
            font-size: 1.05rem;
            border-radius: 8px;
            transition: all 0.3s;
        }

        .nav-item:hover, .nav-item.active {
            background-color: #1c1d27;
            color: #f72585;
        }

        .nav-item i {
            font-size: 1.2rem;
            width: 25px;
        }

        .logout-btn {
            margin-top: auto;
            color: #f72585;
            cursor: pointer;
        }

        /* --- MAIN CONTENT AREA --- */
        .main-content {
            margin-left: 250px;
            flex: 1;
            padding: 40px 20px;
            display: flex;
            justify-content: center;
        }

        .profile-container {
            width: 100%;
            max-width: 750px;
        }

        /* --- PROFILE HEADER INFO --- */
        .profile-header {
            display: flex;
            align-items: center;
            gap: 40px;
            padding-bottom: 40px;
            border-bottom: 1px solid #1f2029;
            margin-bottom: 30px;
        }

        .profile-pic-container {
            position: relative;
        }

        .profile-pic {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #7209b7;
        }

        .profile-info {
            flex: 1;
        }

        .username-row {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 15px;
            flex-wrap: wrap;
        }

        .username-row h2 {
            font-size: 1.4rem;
            font-weight: 600;
        }

        .btn-edit-profile {
            background-color: #1c1d27;
            color: #ffffff;
            border: 1px solid #2a2c3d;
            padding: 6px 16px;
            font-size: 0.9rem;
            font-weight: 600;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .btn-edit-profile:hover {
            background-color: #2a2c3d;
        }

        .stats-row {
            display: flex;
            gap: 30px;
            margin-bottom: 15px;
            font-size: 0.95rem;
        }

        .stats-row span strong {
            color: #ffffff;
        }

        .stats-row span {
            color: #a5a7b4;
        }

        .bio-section h1 {
            font-size: 0.95rem;
            font-weight: 600;
            margin-bottom: 5px;
        }

        .bio-section p {
            font-size: 0.9rem;
            color: #a5a7b4;
            line-height: 1.4;
        }

        /* --- POSTS GRID NAVIGATION --- */
        .grid-nav {
            display: flex;
            justify-content: center;
            border-top: 1px solid transparent;
            margin-bottom: 20px;
        }

        .grid-nav-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 12px 20px;
            color: #ffffff;
            font-size: 0.85rem;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            border-top: 1px solid #ffffff;
            cursor: pointer;
        }

        /* --- POSTS GRID LAYOUT --- */
        .posts-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 4px;
        }

        .grid-post {
            position: relative;
            aspect-ratio: 1 / 1;
            background-color: #12131c;
            overflow: hidden;
            cursor: pointer;
        }

        .grid-post img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s;
        }

        .grid-post:hover img {
            transform: scale(1.05);
        }

        /* Post Hover Overlay */
        .post-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.4);
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            opacity: 0;
            transition: opacity 0.3s;
            color: white;
            font-weight: 600;
        }

        .grid-post:hover .post-overlay {
            opacity: 1;
        }

        /* --- MOBILE BOTTOM NAV --- */
        .bottom-nav {
            display: none;
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background-color: #12131c;
            border-top: 1px solid #1f2029;
            justify-content: space-around;
            padding: 12px 0;
            z-index: 100;
        }

        .bottom-nav a {
            color: #a5a7b4;
            font-size: 1.3rem;
            text-decoration: none;
        }

        .bottom-nav a.active {
            color: #f72585;
        }

        /* --- RESPONSIVE DESIGN --- */
        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }
            .main-content {
                margin-left: 0;
                padding: 15px;
                padding-bottom: 80px;
            }
            .bottom-nav {
                display: flex;
            }
            .profile-header {
                gap: 20px;
                padding-bottom: 25px;
            }
            .profile-pic {
                width: 80px;
                height: 80px;
            }
        }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="brand-logo">ThivashGram</div>
        <ul class="nav-links">
            <li><a href="feed.html" class="nav-item"><i class="fa-solid fa-house"></i> Home Feed</a></li>
            <li><a href="search.html" class="nav-item"><i class="fa-solid fa-magnifying-glass"></i> Search</a></li>
            <li><a href="reels.html" class="nav-item"><i class="fa-solid fa-clapperboard"></i> Reels</a></li>
            <li><a href="messages.html" class="nav-item"><i class="fa-solid fa-paper-plane"></i> Messages</a></li>
            <li><a href="notifications.html" class="nav-item"><i class="fa-solid fa-heart"></i> Notifications</a></li>
            <li><a href="upload.html" class="nav-item"><i class="fa-solid fa-square-plus"></i> Create Post</a></li>
            <li><a href="profile.html" class="nav-item active"><i class="fa-solid fa-user"></i> Profile</a></li>
            <li><a href="settings.html" class="nav-item"><i class="fa-solid fa-gear"></i> Settings</a></li>
            <li><a onclick="logoutUser()" class="nav-item logout-btn"><i class="fa-solid fa-right-from-bracket"></i> Log Out</a></li>
        </ul>
    </nav>

    <main class="main-content">
        <div class="profile-container">
            
            <div class="profile-header">
                <div class="profile-pic-container">
                    <img src="https://via.placeholder.com/150" id="user-avatar-target" class="profile-pic" alt="Avatar">
                </div>
                <div class="profile-info">
                    <div class="username-row">
                        <h2 id="profile-username-header">Loading...</h2>
                        <button class="btn-edit-profile" onclick="window.location.href='settings.html'">Edit Profile</button>
                    </div>
                    <div class="stats-row">
                        <span><strong id="post-count-badge">3</strong> posts</span>
                        <span><strong>1,245</strong> followers</span>
                        <span><strong>120</strong> following</span>
                    </div>
                    <div class="bio-section">
                        <h1 id="bio-display-name">Thivash User</h1>
                        <p id="bio-custom-text">Football | Coding | Editing <br>Live for the game ⚽</p>
                    </div>
                </div>
            </div>

            <div class="grid-nav">
                <div class="grid-nav-item"><i class="fa-solid fa-grid-3x3"></i> Posts</div>
            </div>

            <div class="posts-grid" id="profile-grid-wrapper">
                
                <div class="grid-post">
                    <img src="https://images.unsplash.com/photo-1508098682722-e99c43a406b2?q=80&w=600&auto=format&fit=crop" alt="Post">
                    <div class="post-overlay">
                        <span><i class="fa-solid fa-heart"></i> 1.2K</span>
                        <span><i class="fa-solid fa-comment"></i> 32</span>
                    </div>
                </div>

                <div class="grid-post">
                    <img src="https://images.unsplash.com/photo-1540747737956-37872e7e56ae?q=80&w=600&auto=format&fit=crop" alt="Post">
                    <div class="post-overlay">
                        <span><i class="fa-solid fa-heart"></i> 985</span>
                        <span><i class="fa-solid fa-comment"></i> 14</span>
                    </div>
                </div>

                <div class="grid-post">
                    <img src="https://images.unsplash.com/photo-1517649763962-0c623066013b?q=80&w=600&auto=format&fit=crop" alt="Post">
                    <div class="post-overlay">
                        <span><i class="fa-solid fa-heart"></i> 740</span>
                        <span><i class="fa-solid fa-comment"></i> 8</span>
                    </div>
                </div>

            </div>

        </div>
    </main>

    <div class="bottom-nav">
        <a href="feed.html"><i class="fa-solid fa-house"></i></a>
        <a href="search.html"><i class="fa-solid fa-magnifying-glass"></i></a>
        <a href="upload.html"><i class="fa-solid fa-square-plus"></i></a>
        <a href="reels.html"><i class="fa-solid fa-clapperboard"></i></a>
        <a href="profile.html" class="active"><i class="fa-solid fa-user"></i></a>
    </div>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
        import { getAuth, onAuthStateChanged, signOut } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-auth.js";

        const firebaseConfig = {
            apiKey: "AIzaSyCR3xMj0rcdC1jRQyGePjtO9rpd8k5Om3E",
            authDomain: "thivashgram.firebaseapp.com",
            projectId: "thivashgram",
            storageBucket: "thivashgram.firebasestorage.app",
            messagingSenderId: "515740477126",
            appId: "1:515740477126:web:44605f2b80c50d86d799c7",
            measurementId: "G-LE2M9GDKCT"
        };

        const app = initializeApp(firebaseConfig);
        const auth = getAuth(app);

        // SYNC USER DATA: Fetch dynamic profiles securely from logged state
        onAuthStateChanged(auth, (user) => {
            if (!user) {
                window.location.href = "index.html";
            } else {
                // Extracting authentic Firebase parameters dynamically
                const userIdent = user.displayName ? user.displayName : user.email.split('@')[0];
                document.getElementById('profile-username-header').innerText = userIdent;
                document.getElementById('bio-display-name').innerText = userIdent;
                
                if(user.photoURL) {
                    document.getElementById('user-avatar-target').src = user.photoURL;
                }
            }
        });

        // Global signout pipeline logic
        window.logoutUser = function() {
            signOut(auth).then(() => {
                window.location.href = "index.html";
            });
        }
    </script>
</body>
</html>

