<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ThivashGram - Admin Control Center</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #0b0c10;
            color: #ffffff;
            display: flex;
            min-height: 100vh;
        }

        /* --- DESKTOP SIDEBAR --- */
        .sidebar {
            width: 250px;
            background-color: #12131c;
            border-right: 1px solid #1f2029;
            padding: 30px 20px;
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
            z-index: 10;
        }

        .brand-logo {
            font-size: 2rem;
            font-weight: bold;
            font-style: italic;
            background: linear-gradient(45deg, #f72585, #7209b7);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 40px;
            padding-left: 10px;
        }

        .nav-links {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 12px 15px;
            color: #a5a7b4;
            text-decoration: none;
            font-size: 1.05rem;
            border-radius: 8px;
            transition: all 0.3s;
        }

        .nav-item:hover, .nav-item.active {
            background-color: #1c1d27;
            color: #f72585;
        }

        .nav-item i {
            font-size: 1.2rem;
            width: 25px;
        }

        .logout-btn {
            margin-top: auto;
            color: #f72585;
            cursor: pointer;
        }

        /* --- ADMIN CONTENT AREA --- */
        .main-content {
            margin-left: 250px;
            flex: 1;
            padding: 40px;
            overflow-y: auto;
        }

        .admin-header {
            font-size: 1.6rem;
            font-weight: 700;
            margin-bottom: 30px;
            border-bottom: 1px solid #1f2029;
            padding-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .admin-header i {
            color: #f72585;
        }

        /* --- ANALYTICS DASHBOARD COUNTERS --- */
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .analytics-card {
            background-color: #12131c;
            border: 1px solid #1f2029;
            border-radius: 12px;
            padding: 25px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }

        .card-info h3 {
            font-size: 0.9rem;
            color: #a5a7b4;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
        }

        .card-info p {
            font-size: 1.8rem;
            font-weight: 700;
        }

        .card-icon {
            width: 50px;
            height: 50px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.4rem;
        }

        .card-icon.users { background-color: rgba(114, 9, 183, 0.15); color: #7209b7; }
        .card-icon.posts { background-color: rgba(247, 37, 133, 0.15); color: #f72585; }
        .card-icon.reports { background-color: rgba(255, 183, 3, 0.15); color: #ffb703; }

        /* --- CONTROL MANAGEMENT TABLES --- */
        .management-section {
            background-color: #12131c;
            border: 1px solid #1f2029;
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 30px;
        }

        .section-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: #e2e2e5;
        }

        .table-responsive {
            width: 100%;
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            text-align: left;
            font-size: 0.9rem;
        }

        th, td {
            padding: 14px 16px;
            border-bottom: 1px solid #1f2029;
        }

        th {
            background-color: #1c1d27;
            color: #a5a7b4;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 0.5px;
        }

        td {
            color: #e2e2e5;
        }

        .td-user {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .td-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            object-fit: cover;
        }

        /* Action Buttons */
        .btn-ban {
            background-color: transparent;
            border: 1px solid #f72585;
            color: #f72585;
            padding: 6px 12px;
            border-radius: 4px;
            font-weight: 600;
            font-size: 0.8rem;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-ban:hover {
            background-color: #f72585;
            color: white;
        }

        .btn-delete {
            background-color: #f72585;
            color: white;
            border: none;
            padding: 6px 12px;
            border-radius: 4px;
            font-weight: 600;
            font-size: 0.8rem;
            cursor: pointer;
            margin-right: 5px;
        }

        .btn-dismiss {
            background-color: #1c1d27;
            color: #a5a7b4;
            border: 1px solid #2a2c3d;
            padding: 6px 12px;
            border-radius: 4px;
            font-weight: 600;
            font-size: 0.8rem;
            cursor: pointer;
        }

        /* --- RESPONSIVE DESIGN --- */
        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }
            .main-content {
                margin-left: 0;
                padding: 20px 15px;
            }
        }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="brand-logo">ThivashGram</div>
        <ul class="nav-links">
            <li><a href="feed.html" class="nav-item"><i class="fa-solid fa-house"></i> Home Feed</a></li>
            <li><a href="search.html" class="nav-item"><i class="fa-solid fa-magnifying-glass"></i> Search</a></li>
            <li><a href="reels.html" class="nav-item"><i class="fa-solid fa-clapperboard"></i> Reels</a></li>
            <li><a href="messages.html" class="nav-item"><i class="fa-solid fa-paper-plane"></i> Messages</a></li>
            <li><a href="notifications.html" class="nav-item"><i class="fa-solid fa-heart"></i> Notifications</a></li>
            <li><a href="upload.html" class="nav-item"><i class="fa-solid fa-square-plus"></i> Create Post</a></li>
            <li><a href="profile.html" class="nav-item"><i class="fa-solid fa-user"></i> Profile</a></li>
            <li><a href="admin.html" class="nav-item active"><i class="fa-solid fa-user-shield"></i> Admin Panel</a></li>
            <li><a onclick="logoutUser()" class="nav-item logout-btn"><i class="fa-solid fa-right-from-bracket"></i> Log Out</a></li>
        </ul>
    </nav>

    <main class="main-content">
        <div class="admin-header">
            <i class="fa-solid fa-user-shield"></i> ThivashGram Admin Core Dashboard
        </div>

        <div class="dashboard-grid">
            <div class="analytics-card">
                <div class="card-info">
                    <h3>Total Platforms Users</h3>
                    <p id="total-users-count">14,250</p>
                </div>
                <div class="card-icon users"><i class="fa-solid fa-users"></i></div>
            </div>
            <div class="analytics-card">
                <div class="card-info">
                    <h3>Total Live Posts</h3>
                    <p id="total-posts-count">89,412</p>
                </div>
                <div class="card-icon posts"><i class="fa-solid fa-photo-film"></i></div>
            </div>
            <div class="analytics-card">
                <div class="card-info">
                    <h3>Pending Content Reports</h3>
                    <p id="total-reports-count" style="color: #ffb703;">4</p>
                </div>
                <div class="card-icon reports"><i class="fa-solid fa-triangle-exclamation"></i></div>
            </div>
        </div>

        <div class="management-section">
            <div class="section-title">User Account Registry & Moderation</div>
            <div class="table-responsive">
                <table>
                    <thead>
                        <tr>
                            <th>User Profile</th>
                            <th>Email Address</th>
                            <th>Status Flag</th>
                            <th>Moderation Rule</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr id="user-row-1">
                            <td class="td-user">
                                <img src="https://via.placeholder.com/150" class="td-avatar" alt="User">
                                <strong>skate_pro</strong>
                            </td>
                            <td>skate.pro@gmail.com</td>
                            <td style="color: #2ec4b6;">Active</td>
                            <td><button class="btn-ban" onclick="banUserRegistry('user-row-1', 'skate_pro')">Ban User</button></td>
                        </tr>
                        <tr id="user-row-2">
                            <td class="td-user">
                                <img src="https://via.placeholder.com/150" class="td-avatar" alt="User">
                                <strong>nature_explorer</strong>
                            </td>
                            <td>explorer.nature@yahoo.com</td>
                            <td style="color: #2ec4b6;">Active</td>
                            <td><button class="btn-ban" onclick="banUserRegistry('user-row-2', 'nature_explorer')">Ban User</button></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="management-section">
            <div class="section-title">Flagged Content Escalation Queue</div>
            <div class="table-responsive">
                <table>
                    <thead>
                        <tr>
                            <th>Reported Creator</th>
                            <th>Reason For Flag</th>
                            <th>Report Count</th>
                            <th>Executive Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr id="report-row-1">
                            <td><strong>toxic_vibe_99</strong></td>
                            <td style="color: #ffb703;">Abusive Comments In Feed</td>
                            <td>14 reports</td>
                            <td>
                                <button class="btn-delete" onclick="executeContentPurge('report-row-1', 'Purge')">Delete Content</button>
                                <button class="btn-dismiss" onclick="executeContentPurge('report-row-1', 'Dismiss')">Dismiss</button>
                            </td>
                        </tr>
                        <tr id="report-row-2">
                            <td><strong>spammer_bot</strong></td>
                            <td style="color: #ffb703;">Promotional Links Spam</td>
                            <td>42 reports</td>
                            <td>
                                <button class="btn-delete" onclick="executeContentPurge('report-row-2', 'Purge')">Delete Content</button>
                                <button class="btn-dismiss" onclick="executeContentPurge('report-row-2', 'Dismiss')">Dismiss</button>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

    </main>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
        import { getAuth, onAuthStateChanged, signOut } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-auth.js";

        const firebaseConfig = {
            apiKey: "AIzaSyCR3xMj0rcdC1jRQyGePjtO9rpd8k5Om3E",
            authDomain: "thivashgram.firebaseapp.com",
            projectId: "thivashgram",
            storageBucket: "thivashgram.firebasestorage.app",
            messagingSenderId: "515740477126",
            appId: "1:515740477126:web:44605f2b80c50d86d799c7",
            measurementId: "G-LE2M9GDKCT"
        };

        const app = initializeApp(firebaseConfig);
        const auth = getAuth(app);

        // SECURE ROUTER SECURITY ACCESS GUARD
        onAuthStateChanged(auth, (user) => {
            if (!user) {
                window.location.href = "index.html";
            }
            // Pro-tip: Future backend implementation will check if user.uid matches admin credentials mapping in Firestore
        });

        // Account Suspension Controller Module Simulator Logic
        window.banUserRegistry = function(rowId, username) {
            const confirmBan = confirm(`Are you absolutely sure you want to completely suspend and ban user @${username}?`);
            if (confirmBan) {
                document.getElementById(rowId).style.opacity = '0.4';
                const btn = document.querySelector(`#${rowId} .btn-ban`);
                btn.innerText = "Suspended";
                btn.style.backgroundColor = "#f72585";
                btn.style.color = "white";
                btn.disabled = true;
            }
        }

        // Reported Content Pipeline Manager
        window.executeContentPurge = function(rowId, actionType) {
            if(actionType === 'Purge') {
                const confirmPurge = confirm("Purge content permanently from ThivashGram database servers?");
                if(!confirmPurge) return;
            }
            // Smoothly remove row entry from active UI view
            document.getElementById(rowId).style.display = 'none';
        }

        // Global signout system hook execution
        window.logoutUser = function() {
            signOut(auth).then(() => {
                window.location.href = "index.html";
            });
        }
    </script>
</body>
</html>

